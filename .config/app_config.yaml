# Config generated by Boiler; YOU MUST CHANGE THIS.
version: "1"

# Boiler app section
app:
  name: "service_registry"
  version: "v0.0.1"
  description: "Registry для микросервисов"

# Custom keys section
custom:
  github_auth_token:
    group: "github"
    type: "string"
    value: "blank"
    description: "Токен доступа для GitHub"
